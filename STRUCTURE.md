# 프로젝트 지식관리 구조 표준

프로젝트 안의 지식을 AI와 사람이 구분 없이 읽고 쓰는 구조. 이 문서를 기준으로 삼는다.

**표준 갱신일: 2026-08-31**

각 프로젝트의 `pjt-docs/README.md` 상단에도 같은 값을 적어 둔다. 두 값이 다르면 그 프로젝트는
표준보다 뒤처져 있다는 뜻이므로 [update.md](update.md) 절차로 갱신한다. 이 날짜는 프로젝트가
따라와야 하는 변경이 생겼을 때만 올린다. 오타 수정이나 문장 다듬기로는 올리지 않는다.
**이 값을 올릴 때는 `skills/baton-init/templates/baton-hook.mjs`의 `BATON_VERSION` 상수도 같은
값으로 올린다.** 훅이 그 값으로 프로젝트의 뒤처짐을 판단한다 (8장).

## 1. 전체 구조

```
<프로젝트루트>/
  .claude/                      # Claude 도구 설정·플러그인·훅 — 그대로 사용. 지식은 넣지 않는다
  CLAUDE.md                     # 얇은 진입점 (10~20행). 지식 본문 금지
  AGENTS.md                     # 같은 내용 — AGENTS.md 규약을 따르는 도구용
  scripts/check-docs.py         # pjt-docs 정합성 검사 (6장 참조)
  pjt-docs/                     # ★ 지식이 모이는 곳 — AI·사람 공용
    README.md                   # 통합 인덱스: 지식 지도 + 읽는 순서
    CHANGELOG.md                # 릴리스·마일스톤 요약 (선택. 4장 참조)
    HELP.md                     # 개발자용 사용 안내: 폴더 구조, AI 요청 예시, 검사 방법
    overview.md                 # 프로젝트 개요: 목적·배경·기술스택·실행법
    decisions/                  # 결정 기록
      0001-<제목>.md
    domain/                     # 도메인 지식: 업무 규칙·용어·프로세스
    reference/                  # 외부 자료
      원본/                     # 받은 그대로 보존 (수정 금지)
      markdown/                 # 검색 가능한 변환본
    skills/                     # 공유 가능한 노하우 (프로젝트 중립적으로 작성)
    troubleshooting/            # 문제-해결 기록
      YYYY-MM-DD-<제목>.md
    local/                      # git 미추적: 접속정보·개인메모 (.gitignore 등록)
  .baton/                       # ★ 진행 중 작업 상태 + 단위별 수정 이력 — 기본 구성 (8장 참조)
    README.md                   # 운영 규칙 + 이 프로젝트의 배턴 단위 선언
    <이름>/<단위>.md            # 사용자별 폴더 아래, 반복 수정 단위마다 한 장
```

빈 폴더는 만들지 않는다 — 해당 유형의 지식이 처음 생길 때 폴더를 만든다. `pjt-docs/`의 필수 파일은 `README.md`, `HELP.md`, `overview.md` 셋이다. `CHANGELOG.md`는 릴리스나 마일스톤 개념이 있는 프로젝트에서만 둔다.

`pjt-docs/`와 `.baton/`은 함께 간다. 확정된 지식은 `pjt-docs/`에 두고, 아직 진행 중인 작업 상태는 `.baton/`에 둔다. 도입할 때도 갱신할 때도 둘을 같이 처리한다.

`HELP.md`는 **사람 전용** 확인 문서다 — 폴더 구조 그림, AI에게 자주 쓰는 요청 문구, 문서 규칙 읽는 법, 검사 실행법을 심플하게 담는다. 문서 메타데이터·인덱스 등재 의무가 없고(README/CHANGELOG와 같은 취급), 도입 시 템플릿 그대로 복사하면 된다. 프로젝트 전용 스킬·플러그인을 쓰게 되면 그 사용법도 여기에 한 줄씩 추가한다.

## 2. 폴더별 역할

| 폴더 | 담는 것 | 담지 않는 것 |
|---|---|---|
| `decisions/` | 왜 이렇게 하기로 했나, 왜 안 하기로 했나, 언제 다시 논의하나 | 구현 방법 상세 (그건 domain/이나 코드 주석) |
| `domain/` | 업무 규칙, 용어 정의, 프로세스, DB/시스템 도메인 지식 | 일시적 메모 (그건 local/) |
| `reference/원본/` | 설계문서·PPT·명세 등 받은 원본 그대로 | — 수정 금지 |
| `reference/markdown/` | 원본의 검색 가능한 md 변환본 | 원본에 없는 해석 (해석은 domain/에) |
| `skills/` | 다른 프로젝트에 복사해 갈 수 있는 재사용 노하우 | 이 프로젝트에서만 유효한 내용 |
| `troubleshooting/` | 겪은 문제와 해법, 재발 방지법 | 미해결 추측 (해결 후 기록) |
| `local/` | DB 접속정보, 개인 메모 — git 미추적 | 팀과 공유할 지식 |

## 3. README.md — 통합 인덱스 (필수)

AI든 사람이든 이 파일 하나로 전체를 파악한다. 형식:

```markdown
# <프로젝트명> 지식베이스

## 이 프로젝트는
한 단락 요약. 상세는 [overview.md](overview.md).

## 지식 지도
| 문서 | 설명 | 상태 | 갱신일 |
|---|---|---|---|
| [overview.md](overview.md) | 프로젝트 개요 | active | 2026-08-13 |
| [decisions/0001-예시.md](decisions/0001-예시.md) | 예시 결정 | active | 2026-08-13 |

## 읽는 순서
- 처음 온 사람/AI: overview → domain → decisions
- 작업 재개: `.baton/<내 이름>/`의 진행 중인 배턴부터
```

규칙:
- 모든 문서는 지식 지도 표에 한 줄씩 올린다. 표에 없는 문서는 없는 문서다.
- 상태·갱신일은 각 문서의 문서 메타데이터와 일치시킨다.
- 문서가 수십 장 넘게 쌓이는 하위 폴더(변환본 묶음, 분석 결과 트리, 스킬 모음 등)는
  그 폴더에 `_index.md`를 두고 거기서 하위 문서를 등재한다. README 지식 지도에는
  `_index.md` 한 줄만 올린다. `_index.md`가 있는 폴더 아래 문서는 README가 아니라 그
  `_index.md`에 있어야 하며, 정합성 검사가 그것을 확인한다. `_index.md` 자체는 README·
  CHANGELOG와 같은 취급이라 문서 메타데이터 의무가 없다.

## 4. CHANGELOG.md — 릴리스·마일스톤 요약 (선택)

"이 프로젝트에 최근 무엇이 들어갔나"를 팀 전체가 한 파일로 훑는 자리다. 최신이 위.

```markdown
# 변경 이력

## 0.3.0 — 2026-08-30
- 재고 조정 화면 추가. 창고 간 이동까지 한 화면에서 처리
- 로그인 세션 만료를 30분에서 8시간으로. 현장 사용 패턴 반영
- 마감 집계 쿼리 개선. 월마감 소요 시간 12분 → 40초
```

**매 변경마다 쓰는 파일이 아니다.** 릴리스나 마일스톤을 낼 때 그 구간의 변경을 요약해서 한 번
쓴다. 개별 변경이 무엇이었는지는 git 이력과 `.baton/`의 수정 이력에 이미 남아 있고, 같은 것을
손으로 또 옮겨 적으면 이중 기록이 되어 곧 낡는다. 낡은 이력 파일은 없는 것보다 나쁘다.

**릴리스 개념이 없는 프로젝트에는 두지 않는다.** 개발자 한 사람이 git 과 `.baton/`만으로 다른
세션, 다른 PC에서 이어서 작업할 수 있다. 이 파일이 필요해지는 것은 코드를 직접 보지 않는
사람에게 "무엇이 바뀌었나"를 전할 때다.

내용 없는 항목("문서 수정", "버그 수정")은 금지 — 무엇이 왜 바뀌었는지 한 줄로.

## 5. 문서 공통 규칙

모든 `pjt-docs/` 문서 상단에 문서 메타데이터 3필드:

```yaml
---
status: draft | active | deprecated
updated: 2026-08-13
source: 실DB 확인 / 담당자 구두 / 설계문서 v1.2 / 추정
---
```

- `status`: draft(초안, 검증 안 됨) / active(유효) / deprecated(폐기)
- `source`: 이 지식이 어디서 왔나. **검증 방법과 날짜가 있으면 명시** (예: "2026-08-13 실DB 조회로 확인"). 추정이면 솔직하게 `추정`.
- 폐기 시 삭제하지 않는다 — `status: deprecated`로 바꾸고 본문 맨 위에 배너: `> ⚠️ 폐기됨 (2026-08-13). 대체: [새문서](링크)`

`decisions/` 문서는 필드 추가:

```yaml
revisit: 검색 실패율 5% 초과 시 / 제품 버전 업그레이드 시 / 없음(영구)
```

재론 조건을 가능한 한 숫자로. 이게 있어야 같은 논의가 반복되지 않고, AI가 근거 없이 결정을 뒤집지 않는다.

## 6. 운영 규칙

1. **지식 변경 = 같은 커밋에서 README 인덱스 갱신.** 나중에 하면 반드시 어긋난다. 무엇이 왜 바뀌었는지는 커밋 메시지와 배턴의 수정 이력에 남으므로 따로 옮겨 적지 않는다.
2. **코드/동작이 바뀌면 관련 pjt-docs 문서도 같은 커밋에서 갱신.** 틀린 지식은 없는 지식보다 나쁘다 — AI가 자신 있게 틀린 답을 한다.
3. **결정 우선.** 아키텍처·방식 결정 전 `decisions/`를 먼저 확인한다. 기존 결정을 뒤집으려면 그 문서의 `revisit` 조건 충족 여부부터 확인하고, 새 결정은 구현 전에 결정 문서 초안부터 쓴다. (AI가 "왜"를 모르면 그 이유를 리팩토링으로 지워버린다.)
4. **원본은 보존, 작업은 변환본으로.** `reference/원본/`은 손대지 않는다.
5. **AI 도구 폴더에 지식 금지.** `.claude/`는 설정·플러그인·훅 전용. 기존에 있던 지식성 파일은 pjt-docs/로 이관.
6. **파일 변경이 없어도 결론이 나면 남긴다.** 조사·검토가 "하지 않기로 했다"로 끝나면 `decisions/`에(재론 조건 포함), "확인된 사실"로 끝나면 관련 배턴 이력에(여러 단위에 걸치면 `domain/`에도), "우리가 고칠 수 없다"로 끝나면 `troubleshooting/`에 `status: draft`로 적는다. 비용은 들었고 결론은 났는데 파일이 안 바뀌어서 어디에도 안 남는 것이 가장 비싼 손실이다. 같은 조사를 반복하고, 다음 AI가 이미 기각된 안을 다시 제안한다.

### git 은 원격이 없더라도 쓴다

`pjt-docs/`와 `.baton/`은 Markdown 파일이라 git 없이도 읽고 쓸 수 있지만, 변경 이력이 남지
않고 `.baton/`의 Stop 훅도 동작하지 않는다. Stop 훅은 `git status`로 판정하기 때문이다.
그래서 git 저장소가 아닌 프로젝트에 도입할 때는 **원격 없이 `git init`만이라도 하도록 권한다.**
프로토타입이나 초기 버전일수록 되돌릴 지점이 필요하고, 배턴의 수정 이력도 git 이 없으면 이 PC
밖으로 나가지 못한다.

사용자가 원치 않으면 강제하지 않는다. 관리가 필요 없는 단순한 프로젝트일 수 있다. 그 경우
아래 두 가지를 알린다.

- `.baton/`의 Stop 훅이 동작하지 않는다. 세션 시작 때 진행 중인 배턴을 보여주는 안내만 남고,
  배턴 없이 턴이 끝나는 것을 막지 못한다.
- `.gitignore`가 동작하지 않으므로 `pjt-docs/local/`과 `.baton/local/`이 보호되지 않는다.
  백업·공유 대상에서 이 폴더들을 빼야 한다고 사용자에게 알린다.

나중에 git 저장소가 되면 그때까지의 문서가 그대로 첫 커밋에 담긴다. 옮기는 작업은 필요 없다.

### 정합성 검사 (scripts/check-docs.py)

프로젝트 루트에서 `python scripts/check-docs.py` 실행. 검사 항목:
- 문서 메타데이터 누락, `status`/`updated` 필드 누락, 날짜 형식 오류
- README 인덱스 표 ↔ 실제 파일 불일치 (미등재 문서, 없는 파일 링크). `_index.md`가 있는 하위 폴더는 그 `_index.md` 기준으로 같은 검사
- 문서 안 깨진 상대 링크
- `status: active`인데 `updated`가 180일 지난 문서 → 신선도 경고 (`--stale-days`로 조정)

커밋 전이나 작업 시작 시 돌리는 것을 권장. `pjt-docs/local/`과 `reference/원본/`은 검사 제외.

## 7. AI 진입점

### CLAUDE.md (프로젝트 루트)

```markdown
# <프로젝트명>

한 줄 소개.

## 지식베이스
프로젝트 지식은 전부 [pjt-docs/README.md](pjt-docs/README.md)에서 시작한다.
작업 전 반드시 읽을 것: overview.md, 관련 domain/ 문서.

## 규칙
- **IMPORTANT**: 아키텍처·방식 결정 전 `pjt-docs/decisions/`를 먼저 확인할 것. 기존 결정을 뒤집으려면 revisit 조건 충족 여부부터, 새 결정은 구현 전에 결정 문서 초안부터.
- **IMPORTANT**: 지식 변경 시 같은 커밋에서 pjt-docs/README.md 인덱스 갱신. 코드/동작 변경 시 관련 pjt-docs 문서도 같은 커밋에서 갱신.
- **IMPORTANT**: 세션 시작 시 `.baton/local/owner`에 적힌 내 폴더(`.baton/<이름>/`)의 `running`·`waiting` 배턴만 읽고, 진행 중인 것이 있으면 새 작업으로 넘어가기 전에 그것부터 사용자와 정리한다. `owner`가 없으면 `.baton/` 아래 폴더 목록을 보여주고 묻는다. 다른 사람의 폴더는 지시할 때만. 운영 규칙은 `.baton/README.md`.
- **IMPORTANT**: 프로젝트에 남는 작업을 시작하기 전에 내 폴더에서 그 단위의 배턴을 먼저 찾는다. 있으면 수정 이력을 읽고 같은 파일을 이어 쓴다. 없으면 단위 식별자로 새로 만든다.
- **IMPORTANT**: 프로젝트에 남는 작업은 한 턴에 끝났어도 배턴을 갱신하고 수정 이력 항목을 남긴 뒤 `passed`로 닫는다. 파일 변경이 없어도 결론이 난 조사는 남긴다 (6장 6번). 커밋 메시지는 배턴을 대신하지 않고, 닫는 판단은 사용자가 한다.
```

강조 마커(**IMPORTANT**)는 장식이 아니다 — AI의 규칙 준수율을 실제로 높인다. 단, 남발하면 효과가 죽으니 핵심 2~3개에만 쓴다. 진입점은 200행 이하(우리 표준은 20행 이하)로 유지 — 길수록 준수율이 떨어진다.

### AGENTS.md (프로젝트 루트)

CLAUDE.md와 같은 내용. AGENTS.md 규약을 따르는 다른 AI 도구가 자동으로 읽는다.

진입점은 이 둘뿐이다. 도구마다 전용 규칙 파일을 따로 두지 않는다. 같은 내용을 여러 벌 두면
한 곳만 고쳐 놓고 나머지를 잊게 되고, 그때부터 어느 쪽이 맞는지 알 수 없어진다.

## 8. 진행 중 작업 상태와 수정 이력 (.baton/)

`pjt-docs/`는 사람이 정제해서 남긴 확정된 지식을 담는다. 그러나 두 가지가 거기에 없다. 하나는
"무슨 작업을 하다가 세션이 끊겼는가"이고, 다른 하나는 "이 화면을 지난번에 왜 어떻게
고쳤는가"다. 전자는 진행 상태이고 후자는 수정 이력인데, 안정화 단계의 화면 하나에는 수십 번의
수정이 쌓이며 git 이력만으로는 어느 수정이 어느 기능을 왜 건드렸는지 되짚기 어렵다. `.baton/`이
이 둘을 함께 맡는다.

**`.baton/`은 선택 사항이 아니라 표준의 기본 구성이다.** pjt-docs를 도입하는 프로젝트는
`.baton/`도 함께 만들고, 갱신할 때 없으면 그때 만든다.

```
<저장소루트>/
  .baton/
    README.md         # 운영 규칙 + 이 프로젝트의 배턴 단위 선언                git
    <이름>/           # 사용자별 폴더. 개인 프로젝트도 이 구조                 git
      <단위>.md       # 반복 수정 단위마다 한 장 (status: running/waiting/passed)  git
      done/           # 아카이브 (선택)                                        git
    local/            # 이 PC의 사용자 이름(owner) + 로컬 전용 메모             git 제외
    .session/         # 훅이 쓰는 Stop 거부 횟수                                git 제외
```

- **반드시 git에 넣는다.** 수정 이력이 배턴 파일에 쌓이므로 버전관리 밖에 두면 다른 PC와
  팀원에게 보이지 않아 존재 이유가 사라진다. 저장소가 하나면 코드 옆에, pjt-docs가 소스와
  별개의 독립 저장소인 구성이면 그 저장소 안에 둔다. 폴더가 다르므로 확정 지식과 진행 상태가
  섞이지 않는다. `local/`과 `.session/`만 제외한다.
- **사용자별 폴더를 쓴다.** 팀 저장소에서 `.baton/` 하나를 같이 쓰면 남의 진행 상태가 내
  세션에 읽히고 같은 파일을 둘이 갱신해 충돌한다. `.baton/<이름>/`으로 나누고, 이 PC의 이름은
  `.baton/local/owner`에 둔다(git 밖, PC마다 같은 이름을 적는다). 세션 시작과 배턴 검색은
  자기 폴더 안에서만 한다. 다른 사람의 폴더는 사용자가 지시할 때만 연다. 합쳐서 볼지는
  사용자가 판단한다. 개인 프로젝트도 같은 구조를 쓴다. 폴더가 하나뿐일 뿐이고, 규칙이 하나면
  AI가 어느 쪽인지 판단할 일이 없다.
- **파일 하나는 반복해서 손대는 단위 하나다.** 작업 단위가 아니다. 그 단위가 무엇인지(화면
  ID, 섹션, 인터페이스, 산출물, 이관 대상 테이블 등)는 프로젝트마다 다르므로 도입할 때 묻고
  `.baton/README.md`에 선언한다. 파일명은 그 식별자로 짓고 날짜나 작업 동사는 붙이지 않는다.
  화면 안의 조회·저장·결재상신 같은 기능은 파일을 나누지 않고 수정 이력 항목의 두 번째
  토막으로 구분한다. 파일을 기능마다 쪼개면 "이 화면 지난번에 뭐 했지"가 흩어지고, 기능 이름이
  흔들릴 때마다 파일이 새로 생긴다.
- **상태는 덮어쓰고 이력은 쌓는다.** "지금 상태", "다음 할 일", "걸린 것"은 현재 스냅샷이라
  덮어쓴다. "수정 이력"은 `날짜 · 기능 · 요지` 제목 아래 바꾼 것·이유·주의 세 줄을 최신이 위로
  쌓고 지우지 않는다. 시행착오 과정은 적지 않고 결론만 남긴다. `passed`로 닫힌 배턴도 그
  단위를 다시 손대면 `running`으로 되돌려 같은 파일에 이어 쓴다.
- **작업을 시작하기 전에 배턴을 먼저 찾는다.** 프로젝트에 남는 작업이면 자기 폴더에서 그
  단위의 식별자를 찾고, 있으면 수정 이력을 읽고 시작한다. 이 한 단계가 없으면 이력을 쌓아도
  다음 세션이 보지 않는다.
- **세션을 시작할 때는** `.baton/local/owner`로 자기 폴더를 찾고 그 최상위의 `running`과
  `waiting` 배턴만 읽는다. 진행 중인 배턴이 있으면 새 작업으로 넘어가기 전에 그것부터 사용자와
  정리한다. `owner`가 없으면 `.baton/` 아래 폴더 목록을 보여주고 어느 것이 본인 것인지 묻는다.
  폴더가 하나뿐이어도 그것이 자기 것이라고 가정하지 않는다. 팀 저장소에서는 폴더가 여러 개인
  것이 정상이므로 여러 개라는 이유만으로 묻지 않고, 이름이 비슷한 폴더를 스스로 합치지도
  않는다. `owner`는 git 밖에 있으므로 다른 PC에서 클론하면 그 PC에서 손으로 다시 만든다.
  `done/`과 지난 배턴은 지금 요청과 관련이 있을 때만 찾아본다.
- **남기는 기준은 걸린 턴 수가 아니라 프로젝트에 남는 변경인지 여부**다. 한 턴 만에 고치고
  커밋까지 끝냈어도 코드·설정 변경이나 이 프로젝트의 문서 작업이면 배턴을 갱신하고 이력을
  남긴다. 파일 변경이 없어도 결론이 난 조사는 6장 6번 규칙대로 남긴다. 결론 없이 끝난 조사와
  질의응답, 프로젝트와 무관한 산출물에는 만들지 않는다.
- `pjt-docs/`와는 레이어가 다르다. 배턴의 수정 이력은 그 단위의 기록이므로 배턴에 그대로
  둔다. 단위를 넘어 일반화되는 것(업무 규칙, 아키텍처 결정, 재사용할 절차)만 `pjt-docs/`에
  **추가로** 정리하며, 옮겼다고 해서 배턴 원본을 지우지는 않는다.
- **훅이 두 지점에서 거든다.** 규칙이 진입점에 있고 매 턴 로드되는데도, 컨텍스트가 압축되면
  그 규칙이 요약에서 빠지고, 배턴을 전부 닫은 뒤 새 작업이 오면 배턴 없이 커밋까지 가는 일이
  실제로 있었다. 그래서 `scripts/baton-hook.mjs`가 세션 시작 때 owner 와 열린 배턴 목록을
  넣어 주고(SessionStart), 변경이 남아 있는데 열린 배턴도 함께 바뀐 배턴도 없으면 턴 종료를
  거부한다(Stop, 세션당 3회까지). 세션이 끝나는 순간을 알려주는 이벤트가 없어서 Stop 은 세션
  종료가 아니라 매 턴 끝에 확인한다. 어디에서 끊기더라도 직전 턴까지는 기록되게 하려는 것이다.
  턴 도중에 끊겨 그마저 놓친 경우는 다음 세션의 SessionStart 가 "커밋되지 않은 변경이 있는데
  열린 배턴이 없다"로 알려준다. 판정 기준은 git 작업 트리의 변경이되 이번 세션이 만든 것만
  세므로, 파일이 바뀌지 않는 잡담·질의응답·프로젝트 밖 산출물에는 배턴을 요구하지 않고 이전
  세션이 남긴 미커밋 변경 때문에 애먼 턴이 막히지도 않는다. 훅이 보는 것은 배턴이
  열려 있는지뿐이고, 이력의 내용이 충실한지는 보지 않는다.
- **배턴을 닫는 판단은 사용자가 한다.** 커밋했다는 사실은 기능이 완료되었다는 뜻이 아니다.
  작업이 끝났는지 확실하지 않으면 `running`이나 `waiting`으로 두고 다음 세션이 이어받는다.
- **뒤처짐은 훅이 알아챈다.** 훅 스크립트의 `BATON_VERSION` 상수는 이 문서의 "표준 갱신일"과
  같은 값이며, 프로젝트에 복사된 사본은 설치 시점의 값을 들고 있다. SessionStart 가 전역 스킬
  폴더(정본을 pull 하면 갱신된다)의 값과 비교해 어긋나면 세션 첫머리에 알린다. `pjt-docs/`의
  `표준 갱신일`을 모델이 읽고 판단하는 경로와 달리 파일을 직접 비교하므로, 컨텍스트가 압축되어도
  매번 정확하고 `pjt-docs/` 없이 `.baton/`만 있는 프로젝트에서도 걸린다. 알림을 받으면 사용자가
  "업데이트해" 라고 할 때 [update.md](update.md) 절차로 갱신한다. **표준 갱신일을 올릴 때
  `BATON_VERSION` 도 함께 올린다.**
- `check-docs.py`의 정합성 검사 대상은 `pjt-docs/`뿐이고 `.baton/`은 검사하지 않는다. 배턴의
  강제는 위 훅이 맡는다.
- 도입은 손으로 파일을 만들지 말고 `baton-init` 스킬로 한다. 스킬이 사용자 이름과 배턴 단위를
  묻고, `.baton/README.md` 생성, 사용자 폴더 생성, 진입점 규칙 줄 추가, 훅 둘(SessionStart·
  Stop) 등록, `.gitignore` 정리를 한 번에 처리하며, 이미 있는 조각은 건드리지 않는다. 사용자 폴더가 없거나
  `status` 어휘가 다른 구 구조를 만나면 사용자 확인 후 옮긴다. 이 저장소의 `skills/baton-init/`를
  기준으로 삼으며, `~/.claude/skills/baton-init/`로 복사하면 설치된다. 상세한 규약과 필드,
  제약은 그 폴더의 `SKILL.md`와 `templates/baton-readme.md`(설치되는 `.baton/README.md`
  원문)에 적혀 있다.
- pjt-docs 도입은 [adopt.md](adopt.md), 기존 프로젝트 갱신은 [update.md](update.md)를 따르며,
  두 절차 모두 `.baton/`이 없으면 만드는 단계를 포함한다.
